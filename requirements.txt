# AI-Combat-Coach vision pipeline dependencies

# Core CV
# torch + torchvision are installed separately in the Dockerfile from the
# pytorch CPU whl index to ensure the +cpu ABI builds are used.
# Do NOT add torch/torchvision here — ultralytics will honour what's already installed.
ultralytics>=8.2.0          # YOLOv8-pose + BoT-SORT tracker
opencv-python-headless>=4.9  # frame extraction, no GUI needed
numpy>=1.26
lap>=0.5.12                  # BoT-SORT / ByteTrack lap assignment (ultralytics tracker dep)

# Video sources
yt-dlp>=2024.1.0             # YouTube download
boto3>=1.34                  # S3 download

# Supabase
supabase>=2.4.0

# UFC stats scraper (ufc_data_pipeline)
httpx>=0.27.0
beautifulsoup4>=4.12

# Optional: faster ONNX/TensorRT export (comment out if not needed)
# onnx>=1.16
# onnxruntime-gpu>=1.18

# Coach Portal web app
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.0
python-multipart>=0.0.9
